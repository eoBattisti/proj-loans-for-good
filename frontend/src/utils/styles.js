import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme) => ({
  textWrapper: {
    width: "70%",
    fontSize: 16,
    textAlign: "center",
    marginTop: "16px",
    marginBottom: "16px"
  }
}));

export default useStyles;
